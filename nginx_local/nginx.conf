events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type  application/octet-stream;
    server {
        listen 80;

        location /api/ {
            proxy_set_header Host $http_host;
            proxy_pass http://backend:8000/api/;
        }

        location /admin/ {
            proxy_set_header Host $http_host;
            proxy_pass http://backend:8000/admin/;
        }

        location /static/ {
        # TODO Еще раз прочитай и сравни разницу работы с директивой root
            root /staticfiles/;
            index index.html;
        }

        location /api/docs/ {
            alias /usr/share/nginx/html/api/docs/;
            index redoc.html;
        }        
        
        location /media/ {
            alias /media/;
            index redoc.html;
        }

        location / {
            root /staticfiles/;
            index index.html;
        }
    }
}
